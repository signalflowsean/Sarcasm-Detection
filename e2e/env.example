# E2E Test Configuration
# By default, tests target native dev servers (npm run dev):
# - Frontend: http://localhost:5173 (Vite dev server)
# - Backend: http://localhost:5000 (Flask debug server)

# Frontend URL (default: http://localhost:5173)
# Set in playwright.config.ts, used globally for page navigation
# E2E_BASE_URL=http://localhost

# Backend API URL (default: http://localhost:5000)
# Used in: smoke.spec.ts, api-health.spec.ts, audio-recording.spec.ts
# E2E_BACKEND_URL=http://localhost:8080

# Moonshine ASR model configuration
# Used in: smoke.spec.ts for model validation and CDN checks
#
# IMPORTANT: Must match VITE_MOONSHINE_MODEL in frontend/env.example
# Uses the same format: model/base or model/tiny
#
# Default changed to base for better speech-to-text accuracy.
# See frontend/docs/MOONSHINE_MODELS.md for model comparison.
#
VITE_MOONSHINE_MODEL=model/base

# Moonshine timeout configuration (in milliseconds)
# These control how long to wait for model loading and VAD initialization.
# E2E tests may need longer timeouts on CI/CD or when testing slow connections.
#
# VITE_MOONSHINE_MODEL_TIMEOUT_MS: Model download/initialization timeout (default: 120000 = 2 minutes)
# VITE_MOONSHINE_VAD_TIMEOUT_MS: VAD initialization timeout (default: 10000 = 10 seconds)
#
# VITE_MOONSHINE_MODEL_TIMEOUT_MS=300000
# VITE_MOONSHINE_VAD_TIMEOUT_MS=15000
