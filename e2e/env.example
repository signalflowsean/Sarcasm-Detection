# E2E Test Environment Variables
# Copy to .env and customize as needed: cp env.example .env

# Frontend URL to test against
E2E_BASE_URL=http://localhost:5173

# Backend URL for API health checks
E2E_BACKEND_URL=http://localhost:5000

# Moonshine model name for smoke test validation
# Options: tiny, base, small
#
# IMPORTANT: Must match VITE_MOONSHINE_MODEL in frontend/env.example.
#
# Format difference:
#   VITE_MOONSHINE_MODEL uses 'model/' prefix → model/tiny
#   MOONSHINE_MODEL_NAME omits the prefix    → tiny
#
MOONSHINE_MODEL_NAME=tiny
