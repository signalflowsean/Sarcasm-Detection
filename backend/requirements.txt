# Web framework
Flask==3.0.0
flask-cors==4.0.0
flask-limiter==3.5.0
gunicorn==21.2.0

# ML dependencies
scikit-learn==1.8.0
numpy>=1.24.0

# Prosodic model dependencies (ONNX Runtime - lightweight inference)
# Replaces PyTorch (~700MB) with ONNX Runtime (~150MB) for smaller Docker image
# The wav2vec2.onnx model file must be present in the backend directory
onnxruntime>=1.16.0

# Audio processing
soundfile>=0.12.0
# scipy is needed for audio resampling (replaces torchaudio)
scipy>=1.11.0

# Development/Testing (not needed in production Docker image)
# Install with: pip install -r requirements.txt -r requirements-dev.txt
