# Development and testing dependencies
# Install with: pip install -r requirements-dev.txt

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0

# Linting and formatting
# Pin ruff version to match pre-commit hook and ensure consistent formatting
ruff==0.14.8

# Pre-commit hooks
pre-commit>=3.8.0
